name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: 
    container:
      image:    
      options: --user root  

    steps:
    - uses: 
    - name: 
      run: 
    - name: 
      run: 
